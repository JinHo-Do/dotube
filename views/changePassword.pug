extends layouts/main

block content
  .form-container
    form(action=`/users${routes.changePassword}`, method="post")
      input(type="password", name="oldPasswod", placeholder="기존 비밀번호")
      input(type="password", name="newPassword", placeholder="새 비밀번호")
      input(type="password", name="newPassword1", placeholder="비밀번호 확인")
      input(type="submit", value="Change Password")
